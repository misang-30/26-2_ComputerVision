**2. 이미지 및 외부 라이브러리 연동 (OpenCV + Qt)**

- **내용:** 프로젝트 이름(`practiceCV`)에서 알 수 있듯 OpenCV 등으로 처리된 영상/이미지를 화면에 보여주는 방법입니다.
    
- **핵심:** OpenCV의 `cv::Mat` 데이터를 Qt에서 출력할 수 있는 `QImage` 및 `QPixmap` 포맷으로 변환 후 `QLabel`에 그리기.